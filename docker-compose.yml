services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./media:/app/media
    environment:
      - DJANGO_SECRET_KEY=change-this-to-a-random-secret-key
      - DJANGO_DEBUG=false
      - DJANGO_ALLOWED_HOSTS=*
    restart: unless-stopped
